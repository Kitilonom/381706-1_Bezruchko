TEST(ПРОЕКТ, НАЗВАНИЕ_ТЕСТА)
{
	ТИП_ОШИБКИ_КАКОЙ_ИСКАТЬ();
}
______________________
Т И П Ы    О Ш И Б О К:

- есть ли ошибка
	ASSERT_ANY_THROW(TVector <int> V(-1));
- сравнить числа
	ASSERT_EQ(3, V.getSize());
- проверить на правду
	ASSERT_TRUE(V1 == V2);
- или на ложь
	ASSERT_FALSE(V1 == V2);
